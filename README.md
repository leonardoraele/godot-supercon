# Super Platformer (WIP)

⚠ This addon is still a work in progress and is missing a lot of critical features.

## Installation

### Via Git

In the project's `addons` folder, do:
```
git submodule add <this_repository_clone_uri> Supercon
```
